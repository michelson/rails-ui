{
  "scripts": {
    "build": "npm install --global yarn && cd test/dummy && npm install && bin/rails tailwindcss:build && bin/rails assets:precompile",
    "heroku-postbuild": "yarn build"
  },
  "packageManager": "npm",
  "engines": {
    "node": "21.x"
  }
}