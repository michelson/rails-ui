{
  "scripts": {
    "build": "cd test/dummy && npm install && SKIP_YARN_INSTALL=true bin/rails assets:precompile --trace",
    "heroku-postbuild": "npm run build"
  },
  "packageManager": "npm",
  "engines": {
    "node": "21.x"
  }
}