{
  "scripts": {
    "build": "SKIP_YARN_INSTALL=true && cd test/dummy && npm install && bin/rails tailwindcss:build && bin/rails assets:precompile",
    "heroku-postbuild": "npm build"
  },
  "packageManager": "npm",
  "engines": {
    "node": "21.x"
  }
}